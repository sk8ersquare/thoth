[package]
name = "thoth"
version = "2026.3.2"
description = "Privacy-first, offline-capable voice transcription application"
authors = ["Paul Roberts"]
license = "MIT"
edition = "2021"

[lib]
name = "thoth_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# Core Tauri
# NOTE: macos-private-api is REQUIRED for transparent windows on macOS - do not remove!
tauri = { version = "2", features = ["tray-icon", "macos-private-api", "image-png"] }
tauri-plugin-global-shortcut = "2"
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-autostart = "2"
tauri-plugin-clipboard-manager = "2"
tauri-plugin-process = "2"
tauri-plugin-os = "2"
tauri-plugin-single-instance = "2"

# Audio
cpal = "0.17"
hound = "3.5"
rubato = "0.15"
crossbeam-channel = "0.5"
webrtc-vad = "0.4"
symphonia = { version = "0.5", features = ["mp3", "aac", "ogg", "vorbis", "flac", "wav", "pcm", "isomp4"] }

# Transcription
# sherpa-rs: Parakeet TDT models via Sherpa-ONNX
# Uses download-binaries to fetch pre-built libs from sherpa-onnx releases.
# macOS: static linking (pre-built .a files, no CRT concerns)
# Windows: dynamic linking (DLLs) — the pre-built static Windows .lib files
#   are compiled with /MT (static CRT), but whisper-rs + Tauri use /MD
#   (dynamic CRT), causing LNK2038. DLLs avoid this entirely.
# Linux: excluded (upstream archive lacks .a files)
sherpa-rs = { version = "0.6", default-features = false, features = ["download-binaries"], optional = true }
# fluidaudio-rs: Apple Neural Engine backend via CoreML (macOS-only, Apple Silicon)
# Forked with platform guards: compiles as no-op on Linux/Windows
fluidaudio-rs = { git = "https://github.com/poodle64/fluidaudio-rs", rev = "97aaf88", optional = true }

# AI Enhancement and Model Downloads
reqwest = { version = "0.12", features = ["json", "stream"] }
futures-util = "0.3"
bzip2 = "0.5"
tar = "0.4"
sha2 = "0.10"
url = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }

# Persistence
rusqlite = { version = "0.32", features = ["bundled"] }

# Keyboard capture (for shortcut recording)
device_query = "2.1"

# Utilities
anyhow = "1.0"
thiserror = "2.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
uuid = { version = "1.0", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
parking_lot = "0.12"
dirs = "6.0"
arboard = "3.4"
enigo = "0.6"
regex = "1.10"
num_cpus = "1.17"

# Desktop-specific (macOS, Windows, Linux)
[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-updater = "2.10"

# macOS-specific
[target.'cfg(target_os = "macos")'.dependencies]
# whisper-rs with Metal GPU acceleration on macOS
whisper-rs = { version = "0.15", features = ["metal"] }
cocoa = "0.26"
objc2 = "0.6"
objc2-foundation = { version = "0.3", features = ["NSGeometry"] }
objc2-app-kit = { version = "0.3", features = ["NSWindow", "NSView", "NSButton", "NSResponder", "NSControl"] }
core-foundation = "0.10"
core-graphics = "0.24"

[features]
default = ["parakeet-static", "fluidaudio"]
parakeet = ["sherpa-rs"]
# parakeet-static: static linking (macOS). Avoids bundling .dylibs.
# Windows must use "parakeet" (dynamic) because pre-built sherpa-onnx
# static .lib files use /MT CRT which conflicts with whisper-rs /MD.
parakeet-static = ["sherpa-rs", "sherpa-rs/static"]
fluidaudio = ["fluidaudio-rs"]
# GPU acceleration for whisper.cpp (mutually exclusive, only use one)
# Use: cargo build --features cuda      for NVIDIA GPUs
# Use: cargo build --features hipblas   for AMD GPUs (ROCm/HIP)
# Use: cargo build --features vulkan    for cross-platform Vulkan (experimental)
cuda = ["whisper-rs/cuda"]
# CUDA acceleration for Parakeet on Windows (NVIDIA GPUs)
parakeet-cuda = ["parakeet", "sherpa-rs/cuda"]
hipblas = ["whisper-rs/hipblas"]
vulkan = ["whisper-rs/vulkan"]

# Linux-specific
[target.'cfg(target_os = "linux")'.dependencies]
# whisper-rs - GPU features enabled via --features flag
whisper-rs = { version = "0.15", default-features = false }
# XDG Desktop Portal for Wayland global shortcuts
ashpd = { version = "0.11", default-features = false, features = ["tokio"] }
# X11 support for display server detection and mouse tracking
x11rb = { version = "0.13", features = ["allow-unsafe-code"] }

# Windows — CPU Whisper (+ optional CUDA/Vulkan GPU via --features)
# whisper-rs default features include the GGML CPU backend; no extra flags needed.
# sherpa-rs (Parakeet) is declared in [dependencies] as optional and is
# activated via the `parakeet` feature, which is now enabled in Windows CI.
[target.'cfg(target_os = "windows")'.dependencies]
whisper-rs = { version = "0.15" }
# Win32 APIs for mouse tracking (GetCursorPos, GetDeviceCaps) and DPI awareness
windows = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_Graphics_Gdi",
] }
[dev-dependencies]
tempfile = "3.25"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true

# Patch sherpa-rs-sys to add missing Windows system lib (advapi32).
# ONNX Runtime ETW telemetry + espeak-ng registry access require it;
# the upstream crate doesn't link it. Remove once sherpa-rs-sys >= 0.6.9 fixes this.
[patch.crates-io]
sherpa-rs-sys = { path = "../vendor/sherpa-rs-sys" }
